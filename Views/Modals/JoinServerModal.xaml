<Window x:Class="isegoria_wpf.Views.Modals.JoinServerModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JoinServerModal"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="600"
        Height="300"
        xmlns:vm="clr-namespace:isegoria_wpf.ViewModels">

    <Window.DataContext>
        <vm:ServerViewModel />
    </Window.DataContext>

    <Grid>
        <Border CornerRadius="12"
                Width="600"
                Height="300"
                Background="{StaticResource BackgroundColor}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                ClipToBounds="True"
                Padding="24"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center"
                        Margin="40,0">

                <!-- 타이틀 -->
                <TextBlock Text="서버 참여하기"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,24" />

                <!-- 서버 이름 -->
                <TextBlock Text="서버 코드"
                           Foreground="White"
                           FontSize="12"
                           FontFamily="{StaticResource Pretendard}"
                           Margin="0,0,0,8" />

                <TextBox x:Name="ServerNameInput"
                         Style="{StaticResource InputBoxStyle}"
                         Text="{Binding ServerCode, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           FontSize="11"
                           Margin="0,4,0,0" />

                <!-- 안내 문구 -->
                <TextBlock Text="서버에 참여하면 ISEGORIA 이용약관 및 해당 서버의 규칙에 동의하는 것으로 간주됩니다."
                           Foreground="White"
                           FontSize="11"
                           Margin="0,8,0,32" />

                <!-- 버튼 -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Height="44"
                            Cursor="Hand"
                            Click="Cancel_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="CancelBorder"
                                        Background="#2A2640"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1"
                                        CornerRadius="10">
                                    <TextBlock Text="취소"
                                               Foreground="White"
                                               FontSize="14"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver"
                                             Value="True">
                                        <Setter TargetName="CancelBorder"
                                                Property="Background"
                                                Value="#3A3650" />
                                    </Trigger>
                                    <Trigger Property="IsPressed"
                                             Value="True">
                                        <Setter TargetName="CancelBorder"
                                                Property="Background"
                                                Value="#1A1630" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2"
                            Content="참여"
                            Height="44"
                            Cursor="Hand"
                            Command="{Binding JoinServerCommand}"
                            Style="{StaticResource LoginButtonStyle}" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>